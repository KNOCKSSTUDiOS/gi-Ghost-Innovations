{
  "name": "gi-platform",
  "version": "1.0.0",
  "description": "G.I. Global Intelligence Platform — Core Server",
  "main": "server.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only server.ts",
    "start": "node dist/server.js",
    "build": "tsc"
  },
  "dependencies": {
    "express": "^4.19.2",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  }
}

